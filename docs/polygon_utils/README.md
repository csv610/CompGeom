# `polygon_utils.py`

Purpose: compatibility re-export for polygon algorithms.

Algorithm: no direct implementation. Re-exports `trianglemesh.polygon`.

Use: keeps older scripts working while the main code lives in `trianglemesh.polygon`.
