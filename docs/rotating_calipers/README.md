# `rotating_calipers.py`

Purpose: CLI for farthest-pair search on planar points.

Algorithm:
- computes the convex hull first
- applies rotating calipers to test antipodal pairs

Input: one `x y` point per line.

Output: farthest pair and distance.
