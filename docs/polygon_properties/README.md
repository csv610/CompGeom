# `polygon_properties.py`

Purpose: CLI for basic polygon measurements.

Algorithm:
- applies the shoelace formula
- computes signed area, centroid, and orientation

Input: polygon vertices `x y`, one per line.

Output: area, centroid, and orientation.
