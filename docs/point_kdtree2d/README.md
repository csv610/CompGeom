# `point_kdtree2d.py`

Purpose: CLI for building and displaying a 2D KD-tree.

Algorithm:
- recursively sorts points by alternating x/y axes
- chooses the median as the split point at each depth

Input: one `x y` point per line.

Output: textual tree display.
