# `smallest_enclosing_circle.py`

Purpose: CLI for the minimum enclosing circle of a point set.

Algorithm:
- randomizes the input order
- applies Welzl's randomized recursive smallest-enclosing-circle algorithm

Input: one `x y` point per line.

Output: circle center and radius.
