# `random_walker2d.py`

Purpose: CLI for a bounded 2D random walk.

Algorithm:
- repeatedly chooses one valid neighboring cell uniformly at random
- records visit frequencies and final displacement

Input: grid dimensions, start position, and optional step bound via arguments.

Output: walk statistics.
