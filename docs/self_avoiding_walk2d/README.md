# `self_avoiding_walk2d.py`

Purpose: CLI for a self-avoiding walk on a bounded 2D grid.

Algorithm:
- repeatedly chooses an unvisited valid neighbor
- stops on success or when the walk becomes trapped

Input: grid dimensions and start location via command-line arguments.

Output: walk path summary and stopping reason.
