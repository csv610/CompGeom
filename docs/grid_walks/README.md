# `grid_walks.py`

Purpose: compatibility module for grid walks and space-filling curves.

Algorithm: no standalone algorithm in this wrapper. It re-exports implementations from `trianglemesh.walks`.

Use for backward compatibility; prefer `trianglemesh.walks` in new code.
