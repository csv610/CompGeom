# `point_quadtree2d.py`

Purpose: CLI for building and displaying a point quadtree.

Algorithm:
- inserts each point recursively into one of four quadrants relative to a stored point

Input: one `x y` point per line.

Output: textual quadtree display.
