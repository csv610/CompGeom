# `geometry_utils.py`

Purpose: compatibility module for geometry primitives.

Algorithm: no independent algorithm. This file re-exports the implementation from `trianglemesh.geometry`.

Input/Output: not a CLI; import surface only.

Use this when older scripts expect `geometry_utils`, but prefer `trianglemesh.geometry` for new code.
