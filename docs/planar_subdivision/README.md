# `planar_subdivision`

Purpose:
- compatibility shim that re-exports planar subdivision primitives from `trianglemesh.planar`

Notes:
- use this module for legacy-style imports
- new implementations live in `src/trianglemesh/planar.py`
