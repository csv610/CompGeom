# `trianglemesh/__init__.py`

Purpose: top-level package entry point.

Algorithm: no algorithm. It exposes the most common geometry symbols, currently `EPSILON` and `Point`.

Use this as a lightweight import surface when only basic geometry types are needed.
