# `segment_intersection.py`

Purpose: CLI for testing whether two line segments intersect.

Algorithm:
- uses orientation tests and collinearity-on-segment checks

Input: two lines, each `x1 y1 x2 y2`.

Output: boolean intersection result.
