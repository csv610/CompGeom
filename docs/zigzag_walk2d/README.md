# `zigzag_walk2d.py`

Purpose: CLI for generating a zigzag scan path over a grid.

Algorithm:
- traverses anti-diagonals of the grid
- alternates direction on successive diagonals

Input: width and height via command-line arguments.

Output: ordered grid path.
