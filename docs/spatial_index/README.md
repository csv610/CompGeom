# `spatial_index.py`

Purpose: compatibility re-export for spatial data structures.

Algorithm: no direct implementation. Re-exports `trianglemesh.spatial`.

Use: import compatibility only; prefer `trianglemesh.spatial` for new code.
