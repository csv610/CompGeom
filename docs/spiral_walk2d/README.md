# `spiral_walk2d.py`

Purpose: CLI for generating a spiral traversal of a 2D grid.

Algorithm:
- starts from the center or a provided seed
- expands step lengths every two turns to build a spiral path

Input: width, height, and optional start position through arguments.

Output: ordered grid path.
