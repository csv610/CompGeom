# `proximity_utils.py`

Purpose: compatibility re-export for proximity algorithms.

Algorithm: no direct implementation. Re-exports `trianglemesh.proximity`.

Use: import compatibility only; new code should target `trianglemesh.proximity`.
